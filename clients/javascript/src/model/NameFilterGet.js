/**
 * NatCrim Service Integration
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1.3.7
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NameFilterGet model module.
 * @module model/NameFilterGet
 * @version 1.0.0
 */
class NameFilterGet {
    /**
     * Constructs a new <code>NameFilterGet</code>.
     * @alias module:model/NameFilterGet
     * @param matchRequired {Boolean} 
     * @param matchFirstLetters {Number} 
     * @param matchBeginsWith {Boolean} 
     * @param matchFullName {Boolean} 
     */
    constructor(matchRequired, matchFirstLetters, matchBeginsWith, matchFullName) { 
        
        NameFilterGet.initialize(this, matchRequired, matchFirstLetters, matchBeginsWith, matchFullName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, matchRequired, matchFirstLetters, matchBeginsWith, matchFullName) { 
        obj['match_required'] = matchRequired;
        obj['match_first_letters'] = matchFirstLetters;
        obj['match_begins_with'] = matchBeginsWith;
        obj['match_full_name'] = matchFullName;
    }

    /**
     * Constructs a <code>NameFilterGet</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NameFilterGet} obj Optional instance to populate.
     * @return {module:model/NameFilterGet} The populated <code>NameFilterGet</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NameFilterGet();

            if (data.hasOwnProperty('match_required')) {
                obj['match_required'] = ApiClient.convertToType(data['match_required'], 'Boolean');
            }
            if (data.hasOwnProperty('match_first_letters')) {
                obj['match_first_letters'] = ApiClient.convertToType(data['match_first_letters'], 'Number');
            }
            if (data.hasOwnProperty('match_begins_with')) {
                obj['match_begins_with'] = ApiClient.convertToType(data['match_begins_with'], 'Boolean');
            }
            if (data.hasOwnProperty('match_full_name')) {
                obj['match_full_name'] = ApiClient.convertToType(data['match_full_name'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NameFilterGet</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NameFilterGet</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NameFilterGet.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }

        return true;
    }


}

NameFilterGet.RequiredProperties = ["match_required", "match_first_letters", "match_begins_with", "match_full_name"];

/**
 * @member {Boolean} match_required
 */
NameFilterGet.prototype['match_required'] = undefined;

/**
 * @member {Number} match_first_letters
 */
NameFilterGet.prototype['match_first_letters'] = undefined;

/**
 * @member {Boolean} match_begins_with
 */
NameFilterGet.prototype['match_begins_with'] = undefined;

/**
 * @member {Boolean} match_full_name
 */
NameFilterGet.prototype['match_full_name'] = undefined;






export default NameFilterGet;

